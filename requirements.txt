# Full stack: cloud + local-ML backend + GUI. For a lean cloud-only install,
# use `pip install -e .` instead (local ML is opt-in via the [localml] extra).
httpx>=0.27.0
rapidfuzz>=3.6.0
sentence-transformers
beautifulsoup4
playwright
rich
platformdirs>=4.0.0
ttkbootstrap>=1.10.0
python-dotenv
